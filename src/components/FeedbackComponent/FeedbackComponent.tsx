import "./FeedbackComponent.scss";

type Props = {};

const FeedbackComponent = (props: Props) => {
    return (
        <section className="feedback-section">
            <div className="feedback-wrapper"></div>
        </section>
    );
};

export default FeedbackComponent;
